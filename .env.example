DATABASE_URL=postgresql+psycopg2://kasbnoma:kasbnoma@localhost:5432/kasbnoma
APP_ENV=development
# Comma-separated browser origins (the SPA URL(s), not the API host). Extra origins merge with builtins in main.py.
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://k-n-7.onrender.com

# Render production examples:
# APP_ENV=production
# CORS_ORIGINS=https://k-n-7.onrender.com
# DATABASE_URL=<set from Render PostgreSQL>
