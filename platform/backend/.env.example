DATABASE_URL="postgresql://postgres:postgres@localhost:5432/career_platform?schema=public"
PORT=4000

# Required for /api/admin/* (send as `x-admin-key: ...` or `Authorization: Bearer ...`)
ADMIN_API_KEY="change-me"
